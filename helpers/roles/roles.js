const Roles = {
  CUSTOMER: 'customer',
  ADMIN: 'admin',
  DEALER: 'dealer',
};

const RolesKeys = Object.keys(Roles);

const RolesValues = Object.values(Roles);

module.exports = {
  Roles,
  RolesKeys,
  RolesValues,
};
